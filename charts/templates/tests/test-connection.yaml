apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chat-search.fullname" . }}-test-connection"
  labels:
    {{- include "chat-search.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chat-search.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
